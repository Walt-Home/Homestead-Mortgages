# The requirements registry is generated by scripts/build-requirements.mjs and
# verified byte-for-byte against the sheet by `npm run requirements:verify`,
# which runs in CI. Prettier reformatting it makes that check fail — the two
# tools disagree about the file, and the generator is the one that owns it.
packages/requirements/src/generated.ts

# The DU tables, generated by scripts/build-du.mjs from Fannie Mae's spec and
# verified byte-for-byte by `npm run du:verify`. Same reasoning: the generator
# owns these files, and Prettier reformatting them makes that check fail.
packages/du/src/generated/

# Build output and lockfiles.
dist/
**/dist/
package-lock.json

# Doug's kernel, vendored byte for byte from doug-ludlow/Supermortgage (the
# commit is in packages/kernel/VENDORED_FROM). Reformatting it would make every
# re-sync a merge instead of a copy. The 822 KB timer registry beside it is his
# spec's data, not ours to reflow.
packages/kernel/src/kernel/
packages/kernel/spec/

# His xlsx reader, vendored the same way for the tape reader (the commit is in
# packages/partner-book/src/vendored/VENDORED_FROM).
packages/partner-book/src/vendored/
